

## Plano: Ordenar cursos alfabeticamente (A-Z)

Mudança de 1 linha em `src/pages/CursosPage.tsx`: trocar `.order("sort_order")` por `.order("titulo", { ascending: true })`.

| Arquivo | Mudança |
|---------|---------|
| `src/pages/CursosPage.tsx` | Linha 29: `.order("sort_order")` → `.order("titulo", { ascending: true })` |

Nenhuma outra alteração necessária.

