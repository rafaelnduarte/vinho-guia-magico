import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radarjovemdovinho.app',
  appName: 'Radar Jovem do Vinho',
  webDir: 'dist'
};

export default config;
